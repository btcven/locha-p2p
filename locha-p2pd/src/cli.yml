name: locha-p2pd
version: "0.1"
author: Locha Mesh Developers <contact@locha.io>
about: Locha P2P Chat daemon
args:
    - listen-addr:
        short: l
        long: listen-addr
        value_name: multiaddr 
        help: The address where the node will listen for incoming connections.
        default_value: "/ip4/0.0.0.0/tcp/0"
        takes_value: true
    - dial:
        short: d
        long: dial
        value_name: multiaddr
        help: Dial a peer using it's multiaddress.
        multiple: true
    - identity:
        short: id
        long: identity
        value_name: FILE
        help: Identity file path
        default_value: "secret_key"
        takes_value: true
    - allow-ipv4-private:
        long: allow-ipv4-private
        help: Enable discovery of IPv4 private addresses.
    - allow-ipv4-shared:
        long: allow-ipv4-shared
        help: Enable discovery of IPv4 addresses that are part of Shared Address Space (100.64.0.0/10).
    - allow-ipv6-ula:
        long: allow-ipv6-ula
        help: Enable discovery of IPv6 Unique Local Addresses (fc00::/7)
    - use-mdns:
        long: use-mdns
        help: Enable discovery through mDNS. This also enables --allow-ipv4-private and --allow-ipv6-link-local